{
  "name": "card-catalog",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "sass scss/main.scss css/main.css --no-source-map --style=expanded",
    "watch": "sass --watch scss/main.scss css/main.css",
    "test": "node --test"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
